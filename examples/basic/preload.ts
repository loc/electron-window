/**
 * Basic example: Preload script
 *
 * Simply import the library's preload setup to expose the window API.
 */

import "@loc/electron-window/preload";

// You can add additional preload setup here if needed
